import { defaultTheme } from './theme-config'
import { activeRoutes, navbarVisibleRoutes } from './route-config'

export const CONFIG = Object.freeze({
  /**
   * Check theme-config.ts for
   * more information
   */
  defaultTheme,

  /**
   * Check route-config.ts
   * for more information
   */
  activeRoutes,
  navbarVisibleRoutes,
})
